import { ToastOptions } from '@chakra-ui/toast'

const colorScheme = {
  primary: 'black',
  secondary: 'gray',
}

const border = {
  radius: 'md',
}

const toaster = {
  duration: 4000,
  position: 'top',
} as ToastOptions

export { colorScheme, border, toaster }
